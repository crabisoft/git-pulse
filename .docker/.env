# ─────────────────────────────────────────────────────────────────────
# Default Docker environment values (VERSIONED).
# Do NOT put real secrets here. To override a value, create
# .docker/.env.local (git-ignored) — it takes precedence at launch.
# ─────────────────────────────────────────────────────────────────────

# ─── Host ports ──────────────────────────────────────────────────────
POSTGRES_PORT=5432
REDIS_PORT=6379
API_PORT=3001
WEB_PORT=8080
# Vite dev server port (watch mode: npm run docker:dev)
WEB_PORT_DEV=5173
# Component catalogue (make storybook), dev stack only.
STORYBOOK_PORT=6006
# Data browser (make studio), dev stack only.
STUDIO_PORT=5555

# ─── PostgreSQL ──────────────────────────────────────────────────────
POSTGRES_USER=dashboard
POSTGRES_PASSWORD=dashboard
POSTGRES_DB=dashboard
# Database `make smoke` drops and recreates on every run, beside the one above:
# a check must never migrate, and never reset, the data being worked on.
SMOKE_DB=smoke

# ─── API ─────────────────────────────────────────────────────────────
WEB_ORIGIN=http://localhost:8080
# Master key location inside the container (master-key volume).
MASTER_KEY_FILE=/data/master.key
# How much the API says about what it is doing: error < warn < log < debug <
# verbose, each level including the ones before it. Empty means `log`. Set it
# to `debug` to have the version probe name every address it tries.
LOG_LEVEL=debug
# Collection schedule, DORA window and page size live in the database and are
# edited from the Settings section — nothing to set here.

# ─── Web (injected at build time) ────────────────────────────────────
VITE_API_URL=http://localhost:3001
# Hostnames the dev server answers to besides localhost, comma-separated. Needed
# to reach it through a tunnel — which is how a provider's webhooks are tested,
# the delivery landing on /api and being proxied to the back.
VITE_ALLOWED_HOSTS=

# ─── Images ──────────────────────────────────────────────────────────
POSTGRES_IMAGE=postgres:16-alpine
REDIS_IMAGE=redis:7-alpine
# Node image used by the dev containers (watch mode). Tracks the active LTS.
# Debian slim (not Alpine) so Prisma finds libssl/OpenSSL 3.
NODE_IMAGE=node:24-slim
# Base of the screenshots container (make screenshots), built on demand and
# started by nothing else. The tag must be the @playwright/test version in
# front/package.json: the image carries the browser build that version drives,
# and Playwright refuses to run against another.
PLAYWRIGHT_IMAGE=mcr.microsoft.com/playwright:v1.62.1-noble
